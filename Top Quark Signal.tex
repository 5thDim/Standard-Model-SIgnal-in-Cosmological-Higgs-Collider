\documentclass[a4paper,12pt]{article}
\usepackage{amsfonts}
\usepackage{mathrsfs}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage[medium]{titlesec}
\usepackage{bm}
\usepackage{cite}
\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{tikz}
\usepackage[bookmarksnumbered=true,bookmarksopen=true]{hyperref}
\hypersetup{colorlinks,%
            linkcolor=[rgb]{0,0.3,0.6},%
            citecolor=[rgb]{0,0.3,0.6},%
            urlcolor=[rgb]{0,0.3,0.6}}
\usepackage[hmargin=.7in,vmargin=1.1in]{geometry}
\usepackage{indentfirst}

\linespread{1.1}
\allowdisplaybreaks

\titleformat{\section}
{\normalfont\fontsize{15}{20}\bfseries}{\thesection}{1em}{}

\usepackage[T1]{fontenc}
\usepackage{lmodern}

\newcommand{\ii}{\mathrm i}
\newcommand{\dd}{\mathrm d}
\newcommand{\tr}{\operatorname{tr}}
\newcommand{\NL}{\mathrm{NL}}
\newcommand{\bq}{\bm q}
\newcommand{\bk}{\bm k}
\newcommand{\bs}{\bm\sigma}
\newcommand{\one}{\mathbf1_2}
\newcommand{\FP}{\operatorname{FP}}
\newcommand{\G}{\mathcal G}
\newcommand{\cC}{\mathcal C}
\title{\Large\textbf{Weyl Spinor Signal in the Cosmological Higgs Collider}\\[2mm]
\large A direct two-Weyl calculation of the top-quark box}
\author{}
\begin{document}
\date{}
\maketitle


\section{Two-Weyl setup and Feynman rules}
\subsection{Lagrangian, basis and mode functions}
We use $\eta_{\mu\nu}=\operatorname{diag}(1,-1,-1,-1)$,
$\sigma^\mu=(1,\bs)$, $\bar\sigma^\mu=(1,-\bs)$, and
$a(\tau)=-1/(H\tau)$, $\tau<0$. The conformally rescaled fields have action
\begin{align}
 S=\int\dd\tau\dd^3x\big\{&\ii t^\dagger\bar\sigma^\mu\partial_\mu t
 +\ii t^{c\dagger}\bar\sigma^\mu\partial_\mu t^c
 -a(\tau)(m_t+g_t\delta h)(tt^c+t^\dagger t^{c\dagger})\big\},\\
 &g_t=y_t/\sqrt2,\qquad m_t=g_t h_0,\qquad \mu=m_t/H,\qquad N_c=3.
 \label{eq:action}
\end{align}
The spinor epsilon conventions are chosen so that the displayed mass term is
$-a m_t\Psi_W^\dagger\beta_W\Psi_W$, with
\begin{equation}
 \Psi_W=\begin{pmatrix}t_\alpha\\t^{c\dagger\dot\alpha}\end{pmatrix},\qquad
 \beta_W=\begin{pmatrix}0&\one\\\one&0\end{pmatrix},\qquad
 \ii\partial_\tau\Psi_W(\bm p)
 =\begin{pmatrix}-\bs\cdot\bm p&a m_t\one\\a m_t\one&\bs\cdot\bm p\end{pmatrix}\Psi_W(\bm p).
 \label{eq:weyl-eom}
\end{equation}
This column is only a convenient way to keep the two Weyl equations together.
Both entries have the same top charge. In particular no reality condition is imposed.
We evaluate matrices in the orthonormal component basis
\begin{equation}
 e_+=\frac1{\sqrt2}\binom{\one}{\one},\quad
 e_-=\frac1{\sqrt2}\binom{-\one}{\one},\quad
 U_B=(e_+,e_-),\qquad
 \beta=U_B^\dagger\beta_W U_B=\begin{pmatrix}\one&0\\0&-\one\end{pmatrix}.
\end{equation}
This is a change of spinor-component basis, not the replacement of $t,t^c$ by two
Majorana fields. Charge remains proportional to the identity. In this basis,
\begin{equation}
 \mathsf H(\bm p,\tau)=\begin{pmatrix}M&\bs\cdot\bm p\\\bs\cdot\bm p&-M\end{pmatrix},
 \qquad M=-\mu/\tau.
\end{equation}
For $\bs\cdot\hat{\bm p}\,\xi_h=h\xi_h$, $h=\pm1$, use
\begin{equation}
 \xi_+=\binom{e^{-\ii\phi/2}\cos(\theta/2)}{e^{\ii\phi/2}\sin(\theta/2)},\qquad
 \xi_-=\binom{-e^{-\ii\phi/2}\sin(\theta/2)}{e^{\ii\phi/2}\cos(\theta/2)},\qquad
 \sum_h\xi_h\xi_h^\dagger=\one.
\end{equation}
The massless Higgs external contraction is the one used in the supplied notes:
\begin{equation}
 G_a(k;\tau)=\frac{H^2}{2k^3}(1-\ii a k\tau)e^{\ii a k\tau},\qquad a=\pm1.
 \label{eq:higgs}
\end{equation}
All external vectors are incoming, $\sum_i\bk_i=0$, and a prime removes the overall
$(2\pi)^3\delta^3(\sum_i\bk_i)$. We consider exact de Sitter, constant $m_t$, zero
chemical potential, and the leading collapsed expansion.

Write $x=-p\tau>0$. A positive-frequency column has components
$(F(x)\xi_h,hG(x)\xi_h)^T$, giving
\begin{align}
 -\ii F'&=\frac\mu x F+G,& -\ii G'&=F-\frac\mu xG,\\
 F''+\left[1+\frac{\mu^2+\ii\mu}{x^2}\right]F&=0,&
 G''+\left[1+\frac{\mu^2-\ii\mu}{x^2}\right]G&=0.
\end{align}
The Bunch--Davies solutions, with a common irrelevant phase fixed, are
\begin{equation}
 \boxed{F(x)=\frac{\sqrt{\pi x}}2 e^{\pi\mu/2}H^{(1)}_{1/2-\ii\mu}(x),\qquad
 G(x)=\frac{\sqrt{\pi x}}2 e^{-\pi\mu/2}H^{(1)}_{1/2+\ii\mu}(x).}
 \label{eq:modes}
\end{equation}
Both tend to $-\ii e^{\ii x}/\sqrt2$ in the far past. The normalization is
$|F|^2+|G|^2=1$. At fixed Fourier momentum, a useful complete pair of mode matrices is
\begin{equation}
 u(\bm p,\tau)=\binom{F\one}{G\bs\cdot\hat{\bm p}},\qquad
 v(\bm p,\tau)=\binom{G^*\one}{-F^*\bs\cdot\hat{\bm p}}.
 \label{eq:uv}
\end{equation}
The columns multiply independent annihilation and creation operators; the latter
carry antiparticle label $-\bm p$. Thus $u^\dagger u=v^\dagger v=\one$,
$u^\dagger v=0$ and $uu^\dagger+vv^\dagger=\mathbf1_4$ at equal time.
The original Weyl modes are $U_Bu$ and $U_Bv$. This also specifies their relative
phases, which are essential for the soft cut. The mode construction is consistent
with the charged-fermion momentum-space quantization of Ref.~\cite{cota}.

\subsection{SK propagators}
Define the contractions without a prefactor of $\ii$:
\begin{align}
 R^>(p;\tau,\tau')&=u(\tau)u^\dagger(\tau'),&
 R^<(p;\tau,\tau')&=-v(\tau)v^\dagger(\tau').
 \label{eq:wightman}
\end{align}
The minus sign in $R^<$ is the fermionic exchange sign. Explicitly, with
$\sigma_p=\bs\cdot\hat{\bm p}$,
\begin{align}
 R^>&=\begin{pmatrix}F_1F_2^*\one&F_1G_2^*\sigma_p\\G_1F_2^*\sigma_p&G_1G_2^*\one\end{pmatrix},&
 R^<&=\begin{pmatrix}-G_1^*G_2\one&G_1^*F_2\sigma_p\\F_1^*G_2\sigma_p&-F_1^*F_2\one\end{pmatrix}.
 \label{eq:Rblocks}
\end{align}
The four contour contractions are
\begin{align}
 R_{-+}=R^>,\quad R_{+-}=R^<,\qquad
 R_{++}&=\theta(\tau-\tau')R^>+\theta(\tau'-\tau)R^<,\\
 R_{--}&=\theta(\tau'-\tau)R^>+\theta(\tau-\tau')R^<.
 \label{eq:sk}
\end{align}
\subsection{Feynman rules}
The following rules refer to the conformally rescaled fermions in
Eq.~\eqref{eq:action}. SK contractions contain no extra factor of $\ii$.
The branch sign at a vertex will be denoted by $s=\pm1$ here to distinguish it
from the scale factor $a(\tau)$.

\begin{enumerate}
\item \textbf{External Higgs line.} Attach $G_s(k;\tau)$ from
Eq.~\eqref{eq:higgs} to a vertex on branch $s$. External Higgs fields are
labelled before summing permutations.

\item \textbf{Yukawa vertex.} From
$S_{\rm int}=-g_t\int\dd\tau\dd^3x\,a(\tau)\delta h\,
\Psi_W^\dagger\beta_W\Psi_W$, the vertex matrix is
\begin{equation}
 -\ii s\,g_t a(\tau)(\beta_W)_{AB}\,\delta_{cc'}
 =-\ii s\,g_t a(\tau)
 \begin{pmatrix}0&\one\\\one&0\end{pmatrix}_{AB}\delta_{cc'}.
 \label{eq:vertex-rule}
\end{equation}
Here $A$ labels the $\Psi_W^\dagger$ slot, $B$ the $\Psi_W$ slot, and
$c,c'$ are color indices. The two off-diagonal blocks are precisely the
$\delta h\,tt^c$ and $\delta h\,t^\dagger t^{c\dagger}$ vertices with the
index positions specified below. Each has coefficient $-\ii s g_t a(\tau)$;
there is no $1/2$ since the two Weyl fields are distinct. In the mode basis,
replace $\beta_W$ by $\beta=\operatorname{diag}(\one,-\one)$.
There is only one power of the scale factor at a vertex. The background mass
is already included in the propagator, so it is not inserted again.

\item \textbf{Fermion line.} A line with field end $(A,\tau,s)$ and dagger
end $(B,\tau',s')$ contributes
\begin{equation}
 [R_{W,ss'}(\bm p;\tau,\tau')]_{AB}\delta_{cc'}.
\end{equation}
where $R_W=U_BR U_B^\dagger$ and $R$ is given in
Eqs.~\eqref{eq:Rblocks}--\eqref{eq:sk}. The block dictionary is
\begin{equation}
 R_{W,ss'}=
 \begin{pmatrix}
 \langle t_\alpha\,t^\dagger_{\dot\beta}\rangle_{ss'}&
 \langle t_\alpha\,t^{c\beta}\rangle_{ss'}\\
 \langle t^{c\dagger\dot\alpha}\,t^\dagger_{\dot\beta}\rangle_{ss'}&
 \langle t^{c\dagger\dot\alpha}\,t^{c\beta}\rangle_{ss'}
 \end{pmatrix}.
 \label{eq:weyl-line-rule}
\end{equation}
Thus the epsilon tensors needed to raise the $t^c$ indices are already included;
one must not add another epsilon tensor to the vertex matrix. This fixes the
normal and anomalous Weyl contractions in one convention. The charge arrow runs
from the dagger end to the field end, and the Fourier factor of this ordered
line is $e^{\ii\bm p\cdot(\bm x-\bm x')}$.
If its endpoints are exchanged, use the Grassmann identity
\begin{equation}
 \langle T_C\Psi^\dagger_B(\bm p,\tau)\Psi_A(-\bm p,\tau')\rangle_{ss'}
 =-[R_{s's}(-\bm p;\tau',\tau)]_{AB}.
 \label{eq:reverse-line-rule}
\end{equation}
In particular the minus sign of $R^<$ in Eq.~\eqref{eq:wightman} is part of
the line rule, not an optional extra sign. Charge-forbidden contractions such
as $\langle tt\rangle$ and $\langle t^ct^c\rangle$ vanish.

\item \textbf{Integrals and closed loop.} Integrate every vertex over
$\dd\tau\in(-\infty,0)$, impose spatial momentum conservation at each vertex,
and integrate an independent loop momentum with $\dd^3q/(2\pi)^3$.
Use the BD $\ii\epsilon$ prescription for the early-time endpoint, and sum all vertex branch labels. A closed fermion loop gives $-1$, a spin trace,
and a color trace $N_c=3$. No spin average or additional factor of two is
included. Each labelled directed box below has symmetry factor one.
\end{enumerate}
Multiplying these rules gives Eq.~\eqref{eq:each-box}; its overall sign is
$(-1)_{\rm loop}(-\ii)^4=-1$, with the product of the four branch signs kept
inside the SK sum.

\subsection{Nonlocal part of the soft propagator}
For $t=-\tau>0$, the late-time expansion is
\begin{align}
 F(pt)&=A(pt)^{\ii\mu}+\frac{\ii B}{1-2\ii\mu}(pt)^{1-\ii\mu}+O((pt)^2\text{ times the branches}),\\
 G(pt)&=B(pt)^{-\ii\mu}+\frac{\ii A}{1+2\ii\mu}(pt)^{1+\ii\mu}+O((pt)^2\text{ times the branches}),\\
 A&=-\frac{\ii e^{\pi\mu/2}2^{-1/2-\ii\mu}}{\sqrt\pi}\Gamma(\tfrac12-\ii\mu),\quad
 B=-\frac{\ii e^{-\pi\mu/2}2^{-1/2+\ii\mu}}{\sqrt\pi}\Gamma(\tfrac12+\ii\mu),\\
 c&=AB^*=\frac{2^{-1-2\ii\mu}}\pi\Gamma(\tfrac12-\ii\mu)^2,\qquad |c|=\frac1{2\cosh\pi\mu}.
 \label{eq:late}
\end{align}
In particular,
\begin{equation}
 R^{\NL}_{ab}(\bm p;t,t')=
 c(p^2tt')^{\ii\mu}\begin{pmatrix}0&\sigma_p\\0&0\end{pmatrix}
 +c^*(p^2tt')^{-\ii\mu}\begin{pmatrix}0&0\\\sigma_p&0\end{pmatrix}+O(pt,pt').
 \label{eq:soft-leading}
\end{equation}
\textbf{There is no SK label on this expression.} For example, the upper-right
$+\ii\mu$ term is $F(t)G^*(t')$ in $R^>$ and $G^*(t)F(t')$ in $R^<$;
both equal $c(p^2tt')^{\ii\mu}$. This equality also holds for their analytic
momentum descendants. It is compatible with the absence of these powers in the
state-independent anticommutator $R^>-R^<$. This property is independent of the choice between the two-Weyl and
Majorana descriptions. In particular, the reverse Wightman contraction must use
the reversed momentum in its negative-frequency helicity spinors. The two
identities
\begin{align}
 h_s(\bm k)h_s^\dagger(\bm k)
 &=h_{-s}(-\bm k)h_{-s}^\dagger(-\bm k),\\
 [u_s(\tau_1)u_s^*(\tau_2)]_{\NL}
 &=-[v_s(\tau_2)v_s^*(\tau_1)]_{\NL}
\end{align}
then give $[D_{-+}-D_{+-}]_{\NL}=0$ in the Majorana description too.
The project reference PDF is v1 of Ref.~\cite{you-v2}, which used a different
reverse-momentum expression and introduced a nonzero antisymmetric nonlocal
sector. Version v2, Appendix D.3, Eqs.~(237)--(240), explicitly includes
$-\bm k$ and establishes the equality of the two nonlocal Wightman contractions.
Thus the removal of this particular time-ordering obstruction is a correction
to the propagator treatment, not a physical advantage of a Weyl basis. The
fermionic minus sign was already present in v1; attributing the discrepancy
solely to unsigned Wightman conventions would be incorrect. The old project
Majorana box formulas therefore require revision before their final amplitudes
can be compared with this calculation. No independent complete recomputation
of that old box amplitude is asserted here.

For the first nonzero box signal we also need the first descendants. They are
particularly transparent as the following explicit block product:
\begin{equation}
 \left.\beta R^{\NL}(\bm p;t,t')\right|_+
 =c(p^2tt')^{\ii\mu}
 \begin{pmatrix}\one\\-\dfrac{\ii t\bs\cdot\bm p}{1+2\ii\mu}\end{pmatrix}
 \sigma_p
 \begin{pmatrix}-\dfrac{\ii t'\bs\cdot\bm p}{1+2\ii\mu}&\one\end{pmatrix}
 +\cdots .
 \label{eq:soft-factored}
\end{equation}
The other branch is fixed by
$R^{\NL}_-(\bm p;t,t')=[R^{\NL}_+(\bm p;t',t)]^\dagger$.
The dots include second-order corrections to the leading entries. They multiply
a vanishing zeroth-order Bose-symmetrized hard block and do not affect the first
nonzero clock coefficient derived below.

\section{Box diagrams and spin contraction}
\subsection{Diagram counting and four-point integrals}
For four labelled external Higgs fields there are $(4-1)!=6$ directed fermion
cycles, or three undirected boxes with two orientations each. Each carries
$-N_c$, and its symmetry factor is one. Expanding the Dirac determinant gives the
same answer: the $1/4$ cancels cyclic rotations of the $4!$ labelled insertions.
There is no extra Majorana factor, and color is counted once.

For $\pi=(i,j,k,l)$ define
\begin{equation}
 p_{ij}=q+k_i,\qquad p_{jk}=q+k_i+k_j,\qquad
 p_{kl}=q-k_l,\qquad p_{li}=q,
 \label{eq:routing}
\end{equation}
where this equation is a vector equation. The exact individual contribution is
\begin{align}
 \G_{ijkl}={}&-N_cg_t^4\sum_{a_i,a_j,a_k,a_l=\pm}a_i a_j a_k a_l
 \int_{-\infty}^0\prod_{v=i,j,k,l}\big[\dd\tau_v\,a(\tau_v)G_{a_v}(k_v;\tau_v)\big]
 \int\frac{\dd^3q}{(2\pi)^3}\\[-2mm]
 &\hspace{5mm}\times\tr_4\left[
 \beta R_{a_i a_j}(p_{ij};\tau_i,\tau_j)
 \beta R_{a_j a_k}(p_{jk};\tau_j,\tau_k)
 \beta R_{a_k a_l}(p_{kl};\tau_k,\tau_l)
 \beta R_{a_l a_i}(q;\tau_l,\tau_i)\right].
 \label{eq:each-box}
\end{align}
Here $(-\ii)^4=1$ from the four Yukawa vertices. The following table specifies
Eq.~\eqref{eq:each-box} separately for all six diagrams, including all four line
momenta. No unspecified permutation factor is implicit.
\begin{center}\small
\begin{tabular}{ccll}
\toprule
cycle & successive momenta & $s$-soft region & $s$-hard edges\\\midrule
$1234$ & $q+k_1,q+k_s,q-k_4,q$ & $q\sim k_s$ & $12,34$\\
$1243$ & $q+k_1,q+k_s,q-k_3,q$ & $q\sim k_s$ & $12,43$\\
$1324$ & $q+k_1,q+k_1+k_3,q-k_4,q$ & none & ---\\
$1342$ & $q+k_1,q+k_1+k_3,q-k_2,q$ & $q+k_1\sim k_s$ & $34,21$\\
$1423$ & $q+k_1,q+k_1+k_4,q-k_3,q$ & none & ---\\
$1432$ & $q+k_1,q+k_1+k_4,q-k_2,q$ & $q+k_1\sim k_s$ & $43,21$\\
\bottomrule
\end{tabular}\end{center}
\begin{figure}[ht]
\centering
\begin{tikzpicture}[scale=.88,every node/.style={font=\small}]
\foreach \labels/\xx/\yy in {{1,2,3,4}/0/0,{1,2,4,3}/4/0,{1,3,2,4}/8/0,{1,3,4,2}/0/-3,{1,4,2,3}/4/-3,{1,4,3,2}/8/-3}{
 \begin{scope}[shift={(\xx,\yy)}]
  \coordinate (a) at (0,0);\coordinate (b) at (1.6,0);
  \coordinate (c) at (1.6,-1.3);\coordinate (d) at (0,-1.3);
  \draw[<-] (a)--(b);\draw[<-] (b)--(c);\draw[<-] (c)--(d);\draw[<-] (d)--(a);
  \foreach \id [count=\j] in \labels {
   \ifcase\j\or\def\pos{a}\def\dx{-.55}\def\dy{.35}\or\def\pos{b}\def\dx{.55}\def\dy{.35}\or\def\pos{c}\def\dx{.55}\def\dy{-.35}\or\def\pos{d}\def\dx{-.55}\def\dy{-.35}\fi
   \draw (\pos)--++(\dx,\dy) node[above] {$\id$};\fill (\pos) circle (1.5pt);
  }
 \end{scope}
}
\end{tikzpicture}
\caption{The six directed cycles in the order of the table. For the line $R_{ij}$ the charge arrow points from $j$ to $i$, opposite to
the written matrix-cycle order. The Weyl propagator components and vertex chiralities
are summed as in Eq.~\eqref{eq:weyl-components}. The upper right and lower
middle cycles have no two-soft-line cut in the $s$ channel.}
\end{figure}
Here $\bk_s=\bk_1+\bk_2$. Notice $p_{34}=q-k_4=q+k_s+k_3$ in $1234$;
using $q+k_3$ at finite $k_s$ would violate momentum conservation and spoil the
first surviving signal. The $t$- and $u$-channel results follow by relabelling.

For an explicit two-Weyl component enumeration, expand the vertex matrices and propagators.
The trace in each of the six expressions is the sum of the sixteen terms
\begin{equation}
 \mathcal S^{w}_{ijkl}=
 \tr_2[(\beta_W R_{W,ij})^{w_iw_j}(\beta_W R_{W,jk})^{w_jw_k}
 (\beta_W R_{W,kl})^{w_kw_l}(\beta_W R_{W,li})^{w_lw_i}],\qquad w_v\in\{1,2\}.
 \label{eq:weyl-components}
\end{equation}
Each term's four-point function is Eq.~\eqref{eq:each-box} with its trace replaced
by $\mathcal S^w$; its times, loop measure, SK signs and color factor are thus
fully fixed. The component list is
\begin{center}\small
\begin{tabular}{cc@{\qquad}cc}
\toprule
word & successive blocks & word & successive blocks\\\midrule
1111 & 11,11,11,11 & 2111 & 21,11,11,12\\
1112 & 11,11,12,21 & 2112 & 21,11,12,22\\
1121 & 11,12,21,11 & 2121 & 21,12,21,12\\
1122 & 11,12,22,21 & 2122 & 21,12,22,22\\
1211 & 12,21,11,11 & 2211 & 22,21,11,12\\
1212 & 12,21,12,21 & 2212 & 22,21,12,22\\
1221 & 12,22,21,11 & 2221 & 22,22,21,12\\
1222 & 12,22,22,21 & 2222 & 22,22,22,22\\\bottomrule
\end{tabular}\end{center}
These are $96$ algebraic Weyl terms, not $96$ distinct box topologies.
The four $s$-cut cycles contain $64$ of them. Keeping the matrix trace performs
all these component contractions at once and avoids epsilon-index sign ambiguities.

\subsection{Collapsed limit and Bose-symmetry cancellation}
The collapsed kinematics are best organized symmetrically:
\begin{align}
 \bk_1&=\bm k_L+\tfrac12\bk_s,&\bk_2&=-\bm k_L+\tfrac12\bk_s,\\
 \bk_3&=\bm k_R-\tfrac12\bk_s,&\bk_4&=-\bm k_R-\tfrac12\bk_s,\\
 \bm\ell&=\bq+\tfrac12\bk_s,&\bm K&=\bq+\bk_s,\qquad
 \bm n_L=\hat{\bm k}_L,\quad\bm n_R=\hat{\bm k}_R,\quad\bm n_s=\hat{\bk}_s.
 \label{eq:central}
\end{align}
Write $k_L=|\bm k_L|$, $k_R=|\bm k_R|$; at the accuracy of the final answer
$k_L=k_{12}/2$, $k_R=k_{34}/2$, where $k_{ij}=k_i+k_j$ is a sum of magnitudes.

For the $+$ clock branch in $1234$, the two projected hard matrices are
\begin{align}
 V_L&=\begin{pmatrix}-\dfrac{\ii t_1\bs\cdot\bq}{1+2\ii\mu}&\one\end{pmatrix}\beta R_{12}(\bm k_L+\bm\ell;t_1,t_2)\begin{pmatrix}\one\\-\dfrac{\ii t_2\bs\cdot\bm K}{1+2\ii\mu}\end{pmatrix},\\
 V_R&=\begin{pmatrix}-\dfrac{\ii t_3\bs\cdot\bm K}{1+2\ii\mu}&\one\end{pmatrix}\beta R_{34}(\bm k_R+\bm\ell;t_3,t_4)\begin{pmatrix}\one\\-\dfrac{\ii t_4\bs\cdot\bq}{1+2\ii\mu}\end{pmatrix}.
\end{align}
After the Weyl-component sum the entire spin contraction is
\begin{equation}
 c^2(qK)^{2\ii\mu}\prod_v t_v^{\ii\mu}
 \tr_2[V_L\sigma_K V_R\sigma_q].
 \label{eq:spin-cut}
\end{equation}
Thus, already before integration,
\begin{equation}
 \tr[(\bs\cdot\bm A)\sigma_K(\bs\cdot\bm B)\sigma_q]
 =2[(\bm A\cdot\hat K)(\bm B\cdot\hat q)
 +(\bm A\cdot\hat q)(\bm B\cdot\hat K)
 -(\bm A\cdot\bm B)(\hat K\cdot\hat q)].
 \label{eq:pauli}
\end{equation}
At zeroth order $V_L=-R_{21}(k_L)\propto\bs\cdot\bm n_L$ and
$V_R\propto\bs\cdot\bm n_R$. Their spin trace is odd under either
$\bm n_L\to-\bm n_L$ or $\bm n_R\to-\bm n_R$. The sum of the four cut cycles is precisely
the independent exchange of the two left and the two right Higgs fields. Therefore
\begin{equation}
 \boxed{\left.\G^{\rm box}_{s,\rm clock}\right|_{k_s^{3\pm4\ii\mu}}=0.}
 \label{eq:cancellation}
\end{equation}
The cancellation follows before any scalar integration: every zeroth-order
term contains one factor of each hard direction and is odd under either
Higgs-pair exchange. We must therefore keep the first momentum correction at
both ends of the cut.

\section{Scalar integrals for the hard lines and the soft bubble}
\subsection{Left and right hard subgraphs}
We now calculate the first order of each Bose-symmetrized hard block.
Write $x=k_Lt_1$, $y=k_Lt_2$ and $r=p/k_L$. We need only two
SK-summed scalar seeds, corresponding to the $21$ and $11$ blocks of $R$:
\begin{align}
 \mathcal H_{IJ}(\alpha,\beta;r)={}&\sum_{a,b=\pm}ab\int_0^\infty\dd x\dd y\,
 x^{\alpha-1}y^{\beta-1}(1+\ii ax)(1+\ii by)e^{-\ii ax-\ii by}
 [R_{IJ}(r;x,y)]^{\rm radial}_{ab},\nonumber\\
 &\hspace{65mm} IJ=21,11.
 \label{eq:hard-def}
\end{align}
``Radial'' means the coefficient of $\sigma_p$ in $R_{21}$ or of $\one$ in
$R_{11}$; their mode arguments are $rx,ry$. Hence these are scalar integrals,
with the spin structure already removed. Their evaluated series is given
immediately below in Sec.~\ref{sec:seed-eval}.

The useful exact reversal identities are
\begin{equation}
 R_{21;ab}(t_1,t_2)=R_{21;ba}(t_2,t_1),\qquad
 R_{11;ab}(t_1,t_2)=-R_{22;ba}(t_2,t_1).
 \label{eq:reverse}
\end{equation}
For one left ordering, expanding the endpoint projectors gives
\begin{equation}
 V_L=-R_{21}-\frac{\ii t_1}{1+2\ii\mu}\bs\cdot\bq\,R_{11}
                  +\frac{\ii t_2}{1+2\ii\mu}R_{22}\bs\cdot\bm K+O(q^2,k_s^2).
 \label{eq:vertex-expand}
\end{equation}
All entries here are unapproximated hard SK contractions before expansion of their
momentum $\bm p=\bm k_L+\bm\ell$. The other ordering has
$\bm p=-\bm k_L+\bm\ell$. The derivative of the hard line and the descendants
of both soft endpoints must both be kept. Eq.~\eqref{eq:reverse} shows that the
first-order difference between the two external magnitudes gives zero in the
sum: the integrated $R_{21}$ block is symmetric in those energies. Also $\int t_2R_{22}=-\int t_1R_{11}$
at equal external magnitudes.

Strip $H^2/(4k_1^3k_2^3)$ from the left two external lines and vertices. Its
integrated projected matrix, summed over its two Higgs orderings, is
\begin{align}
 \mathcal V_L&=-2k_L^{-1-2\ii\mu}\bs\cdot
 \big[\mathcal P_1(\mu)\bm\ell+
 \mathcal P_2(\mu)\bm n_L(\bm n_L\cdot\bm\ell)\big],\\
 \mathcal P_1(\mu)&=\mathcal H_{21}(\ii\mu,\ii\mu;1)
 +\frac{2\ii}{1+2\ii\mu}\mathcal H_{11}(1+\ii\mu,\ii\mu;1),\\
 \mathcal P_2(\mu)&=\left.(\partial_r-1)\mathcal H_{21}(\ii\mu,\ii\mu;r)\right|_{r=1}.
 \label{eq:hard-result}
\end{align}
The two coefficients $\mathcal P_1,\mathcal P_2$ multiply the independent
spin structures $\bs\cdot\bm\ell$ and
$(\bs\cdot\bm n_L)(\bm n_L\cdot\bm\ell)$.
The $\mathcal H_{11}$ term comes from the soft endpoint descendants;
the $r$ derivative comes from the hard propagator's momentum dependence.
The right result is the same with $(k_L,\bm n_L)\to(k_R,\bm n_R)$ and external
prefactor $H^2/(4k_3^3k_4^3)$. In particular the four-cycle sum is the product
of these two already symmetrized blocks; no further factor of two or four is
allowed. Formula~\eqref{eq:hard-result} retains all terms at this order.
The absence of the constant term in each block makes the signal a spatial
$P$-wave fermion-pair effect. It adds two powers of $k_s$ to the bubble scaling.

\subsection{Evaluation of the scalar hard seeds}
\label{sec:seed-eval}
We give an explicit representation so the answer is a function of mass, rather
than a definition by two unevaluated time integrations. The construction uses only
scalar Hankel functions and ordered scalar Laplace integrals. Its analytic
continuation is part of the definition, just as for scalar double-folded seeds.

A mode $Z\in\{F,G,\bar F,\bar G\}$ has the expansion
\begin{equation}
 Z(rx)=\sum_{\eta=\pm1}\sum_{n=0}^\infty z_{\eta n}(rx)^{\lambda_{\eta n}},\qquad
 \lambda_{\eta n}=\tfrac12+\eta\nu+2n.
 \label{eq:mode-series}
\end{equation}
For $F$ use $(N,\nu)=(\sqrt\pi e^{\pi\mu/2}/2,1/2-\ii\mu)$;
for $G$ use $(\sqrt\pi e^{-\pi\mu/2}/2,1/2+\ii\mu)$. Their coefficients are
\begin{align}
 z_{-n}&=\frac{N}{\ii\sin\pi\nu}
 \frac{(-1)^n2^{\nu-2n}}{n!\Gamma(n+1-\nu)},\\
 z_{+n}&=-\frac{Ne^{-\ii\pi\nu}}{\ii\sin\pi\nu}
 \frac{(-1)^n2^{-\nu-2n}}{n!\Gamma(n+1+\nu)}.
 \label{eq:mode-coeff}
\end{align}
For a barred mode conjugate both coefficients and exponents; $\mu$ is real.
These formulas follow immediately from
$H_\nu^{(1)}=(J_{-\nu}-e^{-\ii\pi\nu}J_\nu)/(\ii\sin\pi\nu)$.

For a monomial $x^{A-1}y^{B-1}$ define two evaluated kernels,
\begin{align}
 K_{\rm all}(A,B;s,t)&=\frac{\Gamma(A)\Gamma(B)}{s^A t^B},\\
 K_{<}(A,B;s,t)&=\frac{\Gamma(A+B)}{A\,t^{A+B}}
 {}_2F_1(A+B,A;A+1;-s/t),\\
 K_{>}(A,B;s,t)&=K_{<}(B,A;t,s).
 \label{eq:Laplace}
\end{align}
The $<$ kernel integrates the region $x<y$, which is $\tau_1>\tau_2$.
It follows by setting $x=zy$, $0<z<1$, and doing the $y$ Gamma integral.

For two specified modes $Z,W$ define the \emph{explicit double series}
\begin{align}
 \mathcal L^{ZW}_{ab,\Omega}(\alpha,\beta;r)
 =\FP_{\delta\to0}\,\operatorname{AC}_{\epsilon\to0^+}
 \sum_{\eta,n;\zeta,m}z_{\eta n}w_{\zeta m}r^{\lambda_{\eta n}+\lambda_{\zeta m}}
 \sum_{j,l=0}^1(\ii a)^j(\ii b)^l\\[-2mm]
 {}\times K_{\Omega}(\alpha+\delta+\lambda_{\eta n}+j,
 \beta+\delta+\lambda_{\zeta m}+l;\epsilon+\ii a,\epsilon+\ii b),
 \qquad \Omega\in\{{\rm all},<,>\}.
 \label{eq:Lseries}
\end{align}
Here AC means analytic continuation of the \emph{summed} function from large
positive Laplace damping (where termwise integration converges) to the displayed
BD boundary value. One must not insert $\epsilon=0$ into a raw power-series
truncation outside its convergence domain. The endpoint regulator $\delta$ is
common to all terms. In the following combinations both regulator operations
are performed \emph{after the full SK sum}. This specifies the finite part
unambiguously and preserves cancellations. For numerical use one may continue
in the Laplace variables in steps, or use the equivalent convergent scalar
Hankel contours; no additional frequency/PV integrations are involved.

The complete SK-summed off-diagonal and diagonal hard seeds are respectively
\begin{align}
 \mathcal H_{21}(\alpha,\beta;r)={}&
 \mathcal L^{G\bar F}_{++,<}+\mathcal L^{\bar F G}_{++,>}
 +\mathcal L^{G\bar F}_{--,>}+\mathcal L^{\bar F G}_{--,<}
 -\mathcal L^{\bar F G}_{+-,\rm all}-\mathcal L^{G\bar F}_{-+,\rm all},\\
 \mathcal H_{11}(\alpha,\beta;r)={}&
 \mathcal L^{F\bar F}_{++,<}-\mathcal L^{\bar G G}_{++,>}
 +\mathcal L^{F\bar F}_{--,>}-\mathcal L^{\bar G G}_{--,<}
 +\mathcal L^{\bar G G}_{+-,\rm all}-\mathcal L^{F\bar F}_{-+,\rm all}.
 \label{eq:hard-series-combo}
\end{align}
All terms on a line share $(\alpha,\beta;r)$. Inserting these two series into
Eq.~\eqref{eq:hard-result} evaluates $\mathcal P_1(\mu),\mathcal P_2(\mu)$.
For $\mathcal P_2$, multiply each term of the
$\mathcal H_{21}(\ii\mu,\ii\mu;1)$ series by
$\lambda_{\eta n}+\lambda_{\zeta m}-1$; no numerical differentiation is needed.
The derivative holds the external Higgs energies fixed.
The regulator limits are taken after the complete SK sum. These are
Gamma--${}_2F_1$ expressions for the two hard coefficients, rather than
unevaluated time integrals; a finite Gamma-only reduction is not assumed.

\subsection{Soft bubble integral}
The spin trace of Eq.~\eqref{eq:pauli} leaves a polynomial numerator divided
by $qK$. To display it, write the two vectors appearing in the hard blocks as
\begin{equation}
 \bm v_L=\mathcal P_1\bm\ell+\mathcal P_2\bm n_L(\bm n_L\cdot\bm\ell),\qquad
 \bm v_R=\mathcal P_1\bm\ell+\mathcal P_2\bm n_R(\bm n_R\cdot\bm\ell).
\end{equation}
Since $\bm K=\bm\ell+\bk_s/2$ and $\bq=\bm\ell-\bk_s/2$, the scalar loop
integral after the spin contraction is
\begin{align}
 &2\int\frac{\dd^3q}{(2\pi)^3}(qK)^{2\ii\mu-1}
 \big[2(\bm v_L\cdot\bm\ell)(\bm v_R\cdot\bm\ell)
 -\tfrac12(\bm v_L\cdot\bk_s)(\bm v_R\cdot\bk_s)\nonumber\\
 &\hspace{55mm}-(\bm v_L\cdot\bm v_R)(\ell^2-k_s^2/4)\big].
 \label{eq:spin-bubble}
\end{align}
Its mass dimension is $5+4\ii\mu$. We now evaluate it by scalar convolutions,
following the same procedure as for the vector box. The only required scalar
formula is
\begin{align}
 F_0(A,B)&=\frac{\Gamma(A+B-\tfrac32)\Gamma(\tfrac32-A)\Gamma(\tfrac32-B)}
 {\Gamma(A)\Gamma(B)\Gamma(3-A-B)},\\
 \int\frac{\dd^3q}{(2\pi)^3}(q^2)^{-A}(K^2)^{-B}
 &=\frac{k_s^{3-2A-2B}}{(4\pi)^{3/2}}F_0(A,B).
 \label{eq:scalar-bubble}
\end{align}
The right-hand side is the analytic continuation of the nonanalytic part;
UV-dependent polynomial contact terms are excluded.

For the reduction only, measure $q,K,\ell$ in units of $k_s$, put
$u=q^2$, $v=K^2$, and set $\nu=1/2-\ii\mu$. Then
\begin{equation}
 \ell^2=(u+v)/2-1/4,\qquad
 \ell_\parallel\equiv\bm\ell\cdot\bm n_s=(v-u)/2,\qquad
 \ell_\perp^2=\ell^2-\ell_\parallel^2.
\end{equation}
Azimuthal averaging replaces two transverse components by
$\ell_\perp^2\delta^\perp_{ij}/2$ and four by
$\ell_\perp^4(\delta^\perp_{ij}\delta^\perp_{kl}
+\delta^\perp_{ik}\delta^\perp_{jl}+\delta^\perp_{il}\delta^\perp_{jk})/8$,
where $\delta^\perp_{ij}=\delta_{ij}-n_{s i}n_{s j}$.
The numerator in Eq.~\eqref{eq:spin-bubble} consequently becomes a polynomial
in $u,v$ and the three external angles. Each monomial is integrated by
\begin{align}
 u^mv^n&\quad\longmapsto\quad
 \frac{F_0(\nu-m,\nu-n)}{(4\pi)^{3/2}},\\
 \frac{F_0(\nu-m,\nu-n)}{F_0(\nu,\nu)}&=
 \frac{(3/2-\nu)_m(3/2-\nu)_n(1-\nu)_m(1-\nu)_n}
 {(5/2-2\nu)_{m+n}(3-2\nu)_{m+n}}.
 \label{eq:bubble-reduction}
\end{align}
Only nonnegative integers $m,n$ with $m+n\leq4$ occur. Thus the bubble is a
finite Gamma sum, and all its terms reduce to the same $F_0(\nu,\nu)$ times
rational functions. For example the coefficient of $\mathcal P_1^2$ in the
square bracket of Eq.~\eqref{eq:spin-bubble} is
$\ell^4+\ell^2/4-\ell_\parallel^2/2$, whose integral is
\begin{equation}
 \frac{F_0(\nu,\nu)}{(4\pi)^{3/2}}
 \frac{3(\nu-1)(2\nu-3)}{8(\nu-2)(4\nu-7)(4\nu-5)}.
\end{equation}
The $\mathcal P_1\mathcal P_2$ and $\mathcal P_2^2$ terms are evaluated by
the same two steps. Their fully reduced coefficients are displayed directly
in Eq.~\eqref{eq:compact-coefficients}, without introducing separate symbols
for each tensor moment.

\section{Nonlocal signal}
\subsection{Oscillatory signal and angular coefficients}
Choose the angle convention
\begin{align}
 \bm n_L&=(\sin\theta_1,0,\cos\theta_1),&
 \bm n_R&=(\sin\theta_3\cos\phi_3,\sin\theta_3\sin\phi_3,\cos\theta_3),&
 \bm n_s&=(0,0,1).
\end{align}
The leading nonzero oscillatory term, summed over the four $s$-cut boxes, is
\begin{align}
 \left.\langle\delta h_{\bk_1}\delta h_{\bk_2}\delta h_{\bk_3}\delta h_{\bk_4}\rangle'
 \right|_{s,\rm clock}
 ={}&-\frac{N_cg_t^4H^4 k_s^5}{16\,k_1^3k_2^3k_3^3k_4^3 k_Lk_R}\nonumber\\
 &\times\left[\left(\frac{k_s^2}{4k_{12}k_{34}}\right)^{2\ii\mu}
 \cC(\mu;\theta_1,\theta_3,\phi_3)+\mathrm{c.c.}\right].
 \label{eq:final}
\end{align}
The coefficient is given directly in the angular basis of the vector-box note:
\begin{align}
 \cC={}&A_1+A_2(\cos^2\theta_1+\cos^2\theta_3)
 +A_3\cos^2\theta_1\cos^2\theta_3\nonumber\\
 &+B_1\sin(2\theta_1)\sin(2\theta_3)\cos\phi_3
 +D_1\sin^2\theta_1\sin^2\theta_3\cos(2\phi_3).
 \label{eq:angular-final}
\end{align}
All five coefficients share one soft-bubble normalization, including the two
soft-mode coefficients $c^2$, the factor $4$ from the two Bose-summed hard blocks, the factor $2$ from the Pauli trace, and the phase from the chosen momentum ratio:
\begin{equation}
 \mathcal B(\mu)=
 \frac{2^{1+4\ii\mu}\Gamma(\tfrac12-\ii\mu)^4 F_0(\nu,\nu)}
 {32\pi^2(4\pi)^{3/2}(\nu-3)(\nu-2)(4\nu-7)(4\nu-5)},\qquad
 \nu=\tfrac12-\ii\mu.
 \label{eq:bubble-normalization}
\end{equation}
Combining the bubble with the two hard coefficients of Eq.~\eqref{eq:hard-result}
gives the final mass dependence:
\begin{align}
 A_1&=\mathcal B(\nu-1)(2\nu-3)
 \big[4(\nu-3)(3\mathcal P_1^2+2\mathcal P_1\mathcal P_2)
 +(2\nu-5)\mathcal P_2^2\big],\\
 A_2&=-\mathcal B\mathcal P_2^2(\nu-1)(2\nu-3)^2,\qquad A_3=-3A_2,\\
 B_1&=-2\mathcal B\mathcal P_2^2(\nu-1)(\nu-2)(2\nu-3),\\
 D_1&=\mathcal B\mathcal P_2^2(\nu-2)(2\nu-5)(2\nu-3).
 \label{eq:compact-coefficients}
\end{align}
Thus the answer requires only two hard functions $\mathcal P_1,\mathcal P_2$
and one explicit bubble factor $\mathcal B$. The coefficients are complex and
include the clock phase. The full answer is even under either Higgs-pair exchange
and under parity. Its engineering dimension is $-5$, as required for the primed
four-point function. The powers of $H$ follow from four rescaled Weyl Yukawa
vertices and four external Higgs contractions; no extra factor of $H$ or a
second color/orientation multiplicity is implicit. Higher soft powers and
analytic contact terms are not displayed.

\subsection{Nonoscillatory contribution}
Taking opposite soft branches gives a nonoscillatory $k_s^3$ term. The hard
blocks are $R_{11}$ and $-R_{22}$; Eq.~\eqref{eq:reverse} makes their integrated
values equal to the real seed $\mathcal H_{11}(-\ii\mu,+\ii\mu;1)$.
The scalar bubble in this case is
\begin{equation}
 \int\frac{\dd^3q}{(2\pi)^3}(q^2)^{-\ii\mu}(K^2)^{\ii\mu}
 \hat q\cdot\hat K
 =\frac{k_s^3}{(4\pi)^{3/2}}\,
 \frac{2\sqrt\pi}{3}\mu(1+\mu^2)\coth(\pi\mu).
\end{equation}
After both soft branches and all four $s$-cut boxes are summed, the result is
\begin{equation}
 \left.\langle\delta h^4\rangle'\right|_{s,\mathrm{mixed}}
 =\frac{N_cg_t^4H^4 k_s^3}{16\prod_i k_i^3}
 \frac{2\mu(1+\mu^2)}{3\pi\sinh(2\pi\mu)}
 \big[\mathcal H_{11}(-\ii\mu,+\ii\mu;1)\big]^2.
 \label{eq:mixed}
\end{equation}
It is independent of angles at this order. Equation~\eqref{eq:mixed} is the
leading nonoscillatory nonanalytic term, whereas Eq.~\eqref{eq:final} is the
leading mass-dependent clock signal. At $\mu=0$ the separated branches coalesce;
the limit must be taken after combining the relevant terms.

\paragraph{Checks and scope.}
The accompanying scripts check the mode equations, normalization, signed SK
contractions, soft descendants, spin traces, scalar Gamma reductions, and hard
momentum expansion. A numerical scan of the analytically continued hard seeds
has not been performed. The calculation assumes constant mass in exact de Sitter,
a massless external Higgs and zero chemical potential. The supplied gauge-boson
note provides the scalar-factorization workflow and angular convention; the
top-quark note provides $g_t,\mu,G_a$. The loop factorization follows
Ref.~\cite{qin}. No Majorana-field decomposition is used.

\begin{thebibliography}{9}
\bibitem{cota} I.~I.~Cot\u aescu, ``Integral representation of the Feynman propagators of
the Dirac fermions on the de Sitter expanding universe,'' Eur. Phys. J. C
\textbf{78}, 769 (2018),
\href{https://doi.org/10.1140/epjc/s10052-018-6258-2}{doi:10.1140/epjc/s10052-018-6258-2}.
\bibitem{you-v2} J.~You, L.~Song, C.~Han, H.-J.~He, X.~Chen and Z.-Z.~Xianyu,
``Cosmological Collider Signatures from Right-Handed Neutrino Loop,''
\href{https://arxiv.org/abs/2605.21419v2}{arXiv:2605.21419v2}, Appendix D.3.
\bibitem{qin} Z.~Qin and Z.-Z.~Xianyu, ``Inflation Correlators at the One-Loop Order:
Nonanalyticity, Factorization, Cutting Rule, and OPE,'' JHEP \textbf{09} (2023) 116,
\href{https://arxiv.org/abs/2304.13295}{arXiv:2304.13295}.
\bibitem{lu} S.~Lu, Y.~Wang and Z.-Z.~Xianyu, ``A Cosmological Higgs Collider,''
JHEP \textbf{02} (2020) 011,
\href{https://arxiv.org/abs/1907.07390}{arXiv:1907.07390}.
\end{thebibliography}
\end{document}
